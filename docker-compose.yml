services:
  mail-hub:
    build: .
    image: baiqigo-mail-hub:local
    ports:
      - "3100:3100"
    volumes:
      - ./data:/app/data
    environment:
      - API_SECRET=${API_SECRET}
      - PORT=3100
      - HOST=0.0.0.0
      - DB_PATH=/app/data/mail.db
    restart: unless-stopped
